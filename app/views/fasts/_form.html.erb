<%= form_for(@fast) do |f| %>
  <% if @fast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fast.errors.count, "error") %> prohibited this fast from being saved:</h2>

      <ul>
      <% @fast.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :where %><br>
    <%= select_tag(:where, options_for_select([Walmart], [Hyvee], [In Town (McNallies, Kum&Go)])) %>
  </div>
  <div class="field">
    <%= f.label :when %><br>
    <%= f.text_field :when %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %>
  </div>
  <div class="field">
    <%= f.label :seats %><br>
    <%= f.text_field :seats %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
