<p id="notice"><%= notice %></p>
<br>
    <div class="container">

<table class="table table-condensed" >

     
      <tr style="background-color:#f5f5f5;">
      <td ><strong><%= @ride.Starting_From %> to <%= @ride.Going_to %></strong><br>
        Depart <%= @ride.depart %> Return  <%= @ride.return %></td>
        <td>&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp$ <%= @ride.Price %> per seat | <%= @ride.Seats %> seats available </td>


    </tr>
      <tr><td><img src="http://maps.google.com/maps/api/staticmap?center=<%= @ride.Starting_From %>&zoom=auto&size=700x350&maptype=roadmap&sensor=false&markers=color:blue|label:A|<%= @ride.Starting_From %>&markers=color:purple|label:B|<%= @ride.Going_to %>" alt="Static, Marked Map of <%= @ride.Starting_From %> and <%= @ride.Going_to %> with no Center and Zoom"></img></td><td><table style="background-color:white;">

        <tr>
        <td><center><%= simple_form_for([@ride, @ride.requests.build]) do |f| %>

&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp&nbsp
<button class="btn btn-success">&nbsp&nbspRequest Ride&nbsp&nbsp</button><% end %></center>
<tr><th>
Contact Information</th></tr>
<tr><td>Name (Year)</td></tr>
<tr><td>Email</td></tr>
<tr><td>Phone Number</td></tr>
<tr><td>Phone Number</td></tr>

</table></td>

        

      </tr> 

      </table>
 
<%= link_to 'Edit', edit_ride_path(@ride) %> |

<%= link_to 'Back', rides_path %><br><br>

<%= render :partial =>"request_list", :locals=>{:requests=> @ride.requests} %>

<div id ="rides">
  <%= render :partial => @ride.requests %><%= @ride.requests.count %>
</div>





